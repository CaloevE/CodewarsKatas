package com.katas.level6

/**
    Created by Evghenii.
    13-January-2022
*/
object EnoughIsEnough {
//    def deleteNth(elements: List[Int], maxOccurrences: Int): List[Int] = {
//         Code here
//      val result = List[Int]
      var nTimes = 0
//      elements.flatMap(x =>if ()) {
        nTimes +=1
//        if (nTimes < maxOccurrences) result(elements) :+ elements

//      }
//      else result

//    }
}
